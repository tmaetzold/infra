[project]
name = "infra"
version = "0.1.0"
description = "Infrastructure configuration and automation scripts"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "click>=8.3.0",
]

[project.scripts]
cli = "infra.cli:cli"

[tool.uv]
package = true
